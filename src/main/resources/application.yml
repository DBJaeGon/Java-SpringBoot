spring:
  application:
    name: nwnt
  config:
    import:
      - classpath:secret/secret-application.properties
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: ${DB_DRIVER}
